[pytest]
markers =
    pet: mark the test as a pet test
    user: mark the test as a user test
    store: mark the test as a store test

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
